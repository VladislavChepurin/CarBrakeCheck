<UserControl x:Class="TechSto.WPF.Views.MeasurementsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TechSto.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border Style="{StaticResource CardBorderStyle}"  Margin="10">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBox x:Name="PortTextBox" Text="COM4"/>
                <Button Content="Подключиться" 
            Command="{Binding ConnectCommand}" 
            CommandParameter="{Binding Text, ElementName=PortTextBox}"/>
                <Button Content="Отключиться" Command="{Binding DisconnectCommand}"/>
                <Button Content="Одиночное измерение" Command="{Binding SingleMeasurementCommand}"/>
                <Button Content="Непрерывное чтение" Command="{Binding StartContinuousCommand}"/>
                <Button Content="Остановить" Command="{Binding StopContinuousCommand}"/>

                <TextBlock Text="{Binding ConnectionStatus}"/>

                <ListBox ItemsSource="{Binding Measurements}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Timestamp, StringFormat='HH:mm:ss'}"/>
                                <TextBlock Text="{Binding TotalWeight, StringFormat='Вес: {0}'}"/>
                                <TextBlock Text="{Binding DiscreteSignals}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
