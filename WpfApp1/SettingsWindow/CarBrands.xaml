<Window x:Class="WpfApp1.SettingsWindow.CarBrands"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1.SettingsWindow"
        xmlns:models="clr-namespace:WpfApp1.DataBase.Entity"
        mc:Ignorable="d"
        Title="CarBrands" Height="300" Width="400"
        Loaded="Window_Loaded"    
        Closed="Window_Closed">
    <!-- Закрывающая скобка после всех атрибутов -->
    <Window.Resources>
        <CollectionViewSource x:Key="BrandsViewSource"/>
    </Window.Resources>

    <Grid Background="#F0F8FF">
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="45"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0"/>
        <Grid Grid.Row="1">
            <DataGrid ItemsSource="{Binding Source={StaticResource BrandsViewSource}}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      Margin="5">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Марка"
                                        Binding="{Binding BrandName}"
                                        Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
        <Grid Grid.Row="2"/>
    </Grid>
</Window>